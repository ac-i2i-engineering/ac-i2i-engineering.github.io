## What changed

<!-- one or two sentences -->

## Checklist

- [ ] `npm run build` passes in `admin/` (if admin code changed)
- [ ] No `.env.local` or real Supabase keys committed
- [ ] If a table/column name changed: `supabase/migrations` and `admin/lib/types.ts` were both updated, and the team was notified
- [ ] Tested manually in the browser
